# Repository Development Rules

在本仓库进行需求规划、代码修改、代码审查或测试设计前，必须先阅读：

1. `docs/development-guardrails.md`
2. 与本次改动直接相关的产品规则和架构文档

涉及异步任务、状态机、父子任务、队列、Worker、轮询、SSE、前端缓存、页面忙碌状态或崩溃恢复时，还必须阅读：

- `docs/async-state-consistency.md`

涉及需求 AI、生图执行计划、图片血缘、主体质检、生成结果状态、素材库或下载能力时，还必须阅读：

- `docs/multi-image-generation-product-rules.md`
- `docs/user-facing-error-policy.md`

开发过程中必须遵守以下要求：

- 将产品规则区分为程序可证明事实、AI 语义判断和生成后视觉判断，不能混为一种“程序校验”。
- AI 负责无法由确定性数据推导的语义选择；程序负责限定合法候选、校验引用和状态，并阻止非法结果进入后续流程。
- 输出级血缘和实体级血缘必须分开建模、分开解析、分开测试，禁止用同一个扁平集合替代实体映射。
- 候选生成成功不等于可交付成功。展示、下载、收藏、再次生成、继续编辑和素材库收录必须使用明确的资产状态与产品规则。
- 不能只验证正常流程。实现前先列出失败和污染场景，实现后按 `docs/development-guardrails.md` 的检查清单逐项核对。
- 关键持久化、血缘、幂等和状态机行为必须使用与生产一致的仓库实现进行验证；内存实现不得与 PostgreSQL 实现产生不同业务语义。
- 每个异步业务问题必须定义唯一权威状态源；缓存和事件不得推翻权威查询成功返回的结果，尤其不得用旧非空缓存覆盖权威空结果。
- 所有跨事务父子状态转换必须设计幂等的崩溃恢复与周期收口，并测试“子任务已终态但父任务未收口”和“仍有子任务运行”两个相反场景。
- 状态测试必须包含跨层不一致组合、刷新、事件断开和进程重启；不能只测试所有层状态一致的正常流程。
- 交付时说明规则对应的代码落点、实际验证范围、未覆盖项和剩余风险。不能用“Prompt 已约束”代替程序能够提供的保证。
